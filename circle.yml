dependencies:
  pre:
    - go get -u github.com/jstemmer/go-junit-report

test:
  override:
      - mkdir -p $CIRCLE_TEST_REPORTS/golang
      - make ci-test


deployment:
  release:
    branch: master
    commands:
      - make build-all
